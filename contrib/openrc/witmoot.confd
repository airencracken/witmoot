# /etc/conf.d/witmoot -- shell syntax; restart Witmoot after editing.
# Native services use 8082 to leave imvault's usual 8080 available.
WITMOOT_ADDR="127.0.0.1:8082"
WITMOOT_DATA_DIR="/var/lib/witmoot"
WITMOOT_NAME="Witmoot"

# Set these before exposing the board through an HTTPS reverse proxy.
# WITMOOT_BASE_URL="https://board.example.org"
# WITMOOT_SECURE_COOKIES="true"
# WITMOOT_TRUSTED_PROXIES="127.0.0.1/32,::1/128"
# WITMOOT_IMVAULT_URL="https://img.internetrelay.chat"

# Service overrides. Paths must be absolute. The installer sets the binary
# default to match PREFIX; uncomment only to override it.
# WITMOOT_BIN="/usr/local/bin/witmoot"
# WITMOOT_USER="witmoot"
# WITMOOT_GROUP="witmoot"
# WITMOOT_LOG_FILE="/var/log/witmoot.log"
# If changing the log path, also edit /etc/logrotate.d/witmoot.
