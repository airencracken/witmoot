#!/bin/sh
# SPDX-License-Identifier: AGPL-3.0-or-later
case "$1" in
	remove|deconfigure)
		if [ -d /run/systemd/system ]; then
			deb-systemd-invoke stop witmoot.service || exit 1
			systemctl disable witmoot.service || exit 1
		fi
		;;
esac
exit 0
